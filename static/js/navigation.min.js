var pageViewNavigation = {
        homeNavWidget: function(n, t) {
            pageViewNavigation.widgetNameactive(n);
            var u = pageViewNavigation.loadWidgetURL(n),
                i = document.getElementsByClassName("SB-matchCardGroup")[0],
                r = document.getElementsByClassName("SB-sectionTitle")[0];
            i !== null && i !== undefined && (i.innerHTML = "");
            r !== null && r !== undefined && (r.innerHTML = "");
            u != "" ? sportOddsTable.loadHomeWidgetOddsData(u, n, t) : i !== null && r !== null && i !== undefined && r !== undefined && (i.innerHTML = "Currently we don't have any events",
                r.innerHTML = t)
        },
        loadWidgetURL: function(n) {
            return n == "popular" ? pageURL = "/home/Populartab" : n == "betbuilder" ? pageURL = "/home/Betbuildertab" : n == "boosted" && (pageURL = ""),
                pageURL
        },
        widgetNameactive: function(n) {
            for (var i = document.getElementsByClassName("SB-filterButtonsItem"), t = 0; t < i.length; t++)
                i[t].classList.remove("active");
            document.getElementById(n).classList.add("active")
        }
    },
    sportOddsTable = {
        loadHomeWidgetOddsData: function(n, t) {
            var r, u, f, i;
            t === "popular" ? (r = document.getElementById("home-betBuilder"),
                r !== null && (r.innerHTML = "",
                    r.classList.add("d-none"))) : t === "betbuilder" && (u = document.getElementById("sb-homewidget"),
                u !== null && (u.innerHTML = ""),
                f = document.getElementById("home-popular"),
                f !== null && f.classList.add("d-none"));
            i = new XMLHttpRequest;
            i.open("GET", n, !0);
            i.onload = function() {
                var r, u, f, n;
                if (i.status >= 200 && i.status < 400)
                    if (t === "popular") {
                        n = document.getElementById("home-betBuilder");
                        n !== null && (n.innerHTML = "",
                            n.classList.add("d-none"));
                        var r = JSON.parse(i.responseText),
                            u = document.getElementById("sb-homewidget"),
                            f = document.getElementById("home-popular");
                        f !== null && f.classList.remove("d-none");
                        u !== null && (u.innerHTML = "",
                            u.innerHTML = r.topgames_carousel,
                            splideCarousel.populatMatchCarousel())
                    } else
                        t === "betbuilder" && (r = JSON.parse(i.responseText),
                            r.home_filterButtons = "",
                            r.upcoming_Data = "",
                            u = document.getElementById("sb-homewidget"),
                            u !== null && (u.innerHTML = ""),
                            f = document.getElementById("home-popular"),
                            f !== null && f.classList.add("d-none"),
                            n = document.getElementById("home-betBuilder"),
                            n !== null && (n.innerHTML = r.home_bbfilterButtons + r.betBuilder_Data,
                                n.classList.remove("d-none")),
                            bbupdatetoLocalTimeZone())
            };
            i.send()
        }
    };